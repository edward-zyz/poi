# 餐饮门店选址辅助工具 (Location Scout) 🚀

欢迎来到 **Location Scout**！这是一个基于高德地图 API 的餐饮选址辅助工具，旨在通过 POI（兴趣点）大数据分析，帮助餐饮从业者科学评估开店位置。

无论你是想学习全栈开发，还是想直接将其投入实战，本指南都将帮助你快速在本地搭建并运行起这套系统。

---

## 🛠 快速上手指南

### 1. 环境准备
在开始之前，请确保你的电脑已安装以下基础环境：
- **Node.js**: 推荐版本 v18.x 或更高 (可以通过 `node -v` 查看)。
- **npm**: 随 Node.js 一起安装。
- **高德地图 API Key**: 你需要前往 [高德开放平台](https://lbs.amap.com/) 注册并申请一个 **Web 服务** 类型的 Key。

### 2. 获取代码与安装依赖
```bash
# 克隆/下载项目后进入目录
cd POI

# 安装全量依赖 (根目录下运行即可，会自动处理前端和后端)
npm install
```

### 3. 核心配置 (关键)
项目需要配置高德地图密钥才能正常工作。

**后端配置：**
1. 找到 `config/gaode.config.json.example` (如果没有，直接创建 `config/gaode.config.json`)。
2. 填写你的 API Key：
   ```json
   {
     "apiKey": "你的高德REST_API_KEY",
     "securityJsCode": "你的安全密钥_JS_CODE"
   }
   ```

**前端配置：**
1. 进入 `frontend` 目录。
2. 创建 `.env.local` 文件并添加：
   ```env
   VITE_GAODE_KEY=你的前端JS_API_KEY
   ```

### 4. 启动服务
建议打开两个终端窗口，分别运行：

- **终端 1 (后端):**
  ```bash
  npm run dev:backend
  ```
- **终端 2 (前端):**
  ```bash
  npm run dev:frontend
  ```

启动成功后，打开浏览器访问 `http://localhost:5173` 即可看到精美的地图交互界面！

---

## 🔮 AI IDE 入门指南 (针对零基础场景)

如果你是一个不懂编程的业务人员，通过 **Cursor** 或 **Windsurf** 等 AI IDE 同样可以丝滑启动本项目。AI 将充当你的“CTO”。

### 1. 软件安装
*   下载并安装 [Cursor](https://cursor.sh/) 或 [Windsurf](https://codeium.com/windsurf)。
*   用它打开本项目的根目录文件夹。

### 2. 使用“对话”启动项目
你不需要背诵任何命令，直接在 IDE 的对话框（一般是 `Cmd+L` 或 `Ctrl+L`）输入：
> **“帮我启动这个项目，并告诉我浏览器的访问地址。”**

AI 会自动执行：
1.  检测并安装缺少的依赖（`npm install`）。
2.  提示你配置高德地图 API Key（如果你还没配置）。
3.  同时启动后端服务（端口 4000）和前端页面（端口 5173）。

### 3. 如何解决报错？
运行过程中如果终端（Terminal）出现了红色的报错，别慌：
*   **方法**：直接用鼠标选中那段报错，按下 `Cmd+L`，AI 会自动读取错误信息并告诉你：*“这是因为你还没有安装 Node.js，请点击这里安装……”* 或者 *“这是因为 API Key 填写不正确，我已经帮你修正了配置文件，请重启试试。”*

### 4. 常见的“魔法指令”示例
你可以尝试对 AI 说：
*   **配置环境**：`“我想配置高德 Key，请告诉我应该改哪个文件，并帮我打开它。”`
*   **修改功能**：`“我觉得热力图的颜色太浅了，帮我改深一点。”`
*   **新增内容**：`“帮我在右侧分析面板下面加一个‘周边停车位分析’的按钮占位符。”`

### 💡 给小白的心法
*   **只要写中文**：AI 完全理解中文需求。
*   **多截图/多附件**：如果页面效果不对，截图丢给 AI，或者输入 `@Files` 选中相关文件让他看。
*   **胆子大一点**：AI 所有的修改你都可以点击 `Discard` 撤销，不用担心改坏。

## 📖 核心功能使用说明

为了让你（或你的朋友）能快速上手，这里列出了 Location Scout 的三大核心使用场景：

### 场景 1：商圈热力大盘（全局洞察）
*   **操作**：在顶部搜索栏输入你想调研的**主品牌**（如：`喜茶`）和**竞品关键词**（如：`奈雪的茶, 霸王茶姬`），点击“生成分析”。
*   **效果**：地图上会立即呈现出品牌的分布热力。
    *   **颜色深浅**代表了门店密集的程度。
    *   通过左侧图层控制，你可以随时开关主品牌或竞品的视觉显示，直观对比“势力范围”。

### 场景 2：目标点位深度穿透（单点评估）
*   **操作**：如果你看中了一个具体的铺位，直接在地图上**点击该位置**。
*   **效果**：
    *   **分析圈**：系统会自动以点击点为中心，画出一个半径 1000 米的分析圈。
    *   **数据面板**：右侧会弹出详尽的报表，展示该点位周边的门店总数、平均密度，以及核心竞品的具体名称。
    *   **智能评分**：根据周边竞争情况自动评估密度等级（高/中/低），辅助你判断这里是“蓝海”还是“红海”。

### 场景 3：选址库管理（决策沉淀）
*   **操作**：在目标点位分析面板中，点击“加入规划”。
*   **效果**：
    *   该点位将被永久收藏到左下角的“点位规划”列表中。
    *   你可以为不同的候选铺位添加备注信息（如：`租金 2W/月`、`带上下水`）。
    *   支持设置 **优先级**（P0-P2），把最有潜力的位置排在前面，方便团队沟通决策。

---

## 🏗 技术架构

- **前端**: React + Vite + Zustand + AMap JS SDK (现代、极速、响应式)
- **后端**: Node.js + Express + TypeScript (结构清晰、类型安全)
- **数据库**: SQLite (轻量级，无需安装数据库软件，即插即用)
- **缓存**: 基于本地 SQLite 的 POI 数据缓存，减少 API 调用频次，降低成本。

---

## 📈 未来规划 (v2.0)

本项目正朝着 SaaS 化迈进，未来将支持：
- 📱 **微信小程序端**: 随时随地实地考察。
- 👥 **多租户体系**: 团队协作，支持多品牌管理。
- 💳 **订阅模式**: 账号等级与 API 配额管理。
- 📊 **深度分析**: 引入人流、商圈消费指数等更多维度。

---

## 🤝 贡献与反馈
如果你在使用过程中遇到任何问题，或者有更好的选址模型建议，欢迎随时交流！

---
*Powered by Antigravity AI Assistant.*
